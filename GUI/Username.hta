<HTML>
    <HEAD>
        <HTA:APPLICATION
  APPLICATIONNAME="ecmd7-LogonHTA"
  ID="ecmd7-LogonHTA"
  VERSION="0.1"
  BORDER="dialog"
  BORDERSTYLE="static"
  ICON="../img/icon2.ico"
  SCROLL="auto"
  SINGLEINSTANCE="yes"
  WINDOWSTATE="maximize"
  CONTEXTMENU="no"
  NAVIGABLE="yes"
  SELECTION="no"/>

        <TITLE>ecmd7 - Please enter your username</TITLE>

        <SCRIPT language="Javascript">
            function closeHTA(sendOutput){
                if (sendOutput){
                    (new ActiveXObject('Scripting.FileSystemObject'))
                        .GetStandardStream(1)
                        .WriteLine(
                            document.getElementById('inputbox').value
                        );
                };
                window.close();
            }
        </SCRIPT>
        <SCRIPT language="vbscript">
        Sub Create_new_account
        Set objShell = CreateObject("WScript.Shell")
        objShell.Run("Create_new_account.hta")
        End Sub
        </SCRIPT>
    </HEAD>

    <BODY> <center>
<img src="../img/logo1.png">
<p>Please enter your username:</p>
<input type="text" id = "inputbox" rows = "4" cols = "36" placeholder="John Doe" class = "valid"><button onclick="closeHTA(true);">Continue</button><br>
<button onclick="Create_new_account()">Create new account</button>
</center></BODY>

</HTML>